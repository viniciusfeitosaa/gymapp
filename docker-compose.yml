# ⚠️ Este arquivo foi substituído por compose files específicos.
#
# Desenvolvimento (hot reload):
#   docker compose -f docker-compose.dev.yml up --build
#   ou: make dev
#
# Produção (homelab):
#   docker compose -f docker-compose.prod.yml up -d --build
#   ou: make prod
#
# Veja HOMELAB_SETUP.md para o guia completo.
