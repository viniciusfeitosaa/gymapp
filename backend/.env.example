# Banco de dados
DATABASE_URL="postgresql://user:password@localhost:5432/gymapp"

# JWT
JWT_SECRET="sua_chave_secreta_jwt"

# Frontend (para URLs de redirect após pagamento)
FRONTEND_URL="http://localhost:5173"

# Asaas (pagamentos / assinatura Plano Pro)
ASAAS_API_KEY="sua_chave_api_asaas"
# Produção (padrão se não definir):
# ASAAS_BASE_URL="https://api.asaas.com"
# ASAAS_CHECKOUT_BASE="https://www.asaas.com"
# Sandbox (para testar sem cobrança real; use chave do painel sandbox):
# ASAAS_BASE_URL="https://api-sandbox.asaas.com"
# ASAAS_CHECKOUT_BASE="https://sandbox.asaas.com"
