# 🔒 NUNCA COMMITE ESTE ARQUIVO COM VALORES REAIS!
# Este é apenas um template. Copie para .env e preencha com valores reais.

# ==========================================
# GYMCONNECT - Variáveis de Ambiente
# ==========================================

# ============ Backend ============
NODE_ENV=development
PORT=3001

# ============ Database - Neon PostgreSQL ============
# ATENÇÃO: NUNCA compartilhe esta URL!
DATABASE_URL="postgresql://SEU_USUARIO:SUA_SENHA@SEU_HOST.neon.tech/neondb?sslmode=require"

# ============ JWT Secret ============
# CRÍTICO: Use uma chave forte em produção!
# Gere com: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=GERE_UMA_CHAVE_FORTE_AQUI_MINIMO_32_CARACTERES

# ============ CORS ============
FRONTEND_URL=http://localhost:5173

# ============ Rate Limiting (Opcional) ============
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ============ Email (Futuro) ============
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=seu_email@gmail.com
# SMTP_PASS=sua_senha_app

# ============ Storage (Futuro) ============
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_BUCKET_NAME=

# ============ Monitoring (Futuro) ============
# SENTRY_DSN=
